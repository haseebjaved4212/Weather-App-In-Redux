const initialState = {
    city : "Karachi",
    current: null,
    forecast: null,
    loading : false,

    error : null
}








const weatherReducer = (state = initialState, action) => {
    if(action.type === "")
};

export default weatherReducer;
